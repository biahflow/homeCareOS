[project]
name = "homecareos-api"
version = "0.1.0"
description = "HomeCareOS API — conferência pré-faturamento de evoluções de prontuário."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
  "fastapi>=0.116,<1",
  "uvicorn[standard]>=0.35,<1",
  "sqlalchemy>=2,<3",
  "alembic>=1.14,<2",
  "psycopg[binary]>=3.2,<4",
  "pydantic-settings>=2.6,<3",
  # Upload multipart das evoluções escaneadas (trilha de intake).
  "python-multipart>=0.0.20,<1",
  # Leitura/renderização de PDF para conferência visual (trilha de extração).
  "pymupdf>=1.26,<2",
  # Cliente S3/MinIO para o storage de documentos (trilha de storage).
  "boto3>=1.40,<2",
  # Extração assistida por IA das evoluções (trilha de extração).
  "anthropic>=0.40,<1",
  # Cliente HTTP do gateway de WhatsApp que envia os alertas (trilha de
  # alertas). Sai do grupo `dev` — onde entrara só para o TestClient do
  # FastAPI — porque agora é dependência de runtime da aplicação.
  "httpx>=0.28,<1",
  # Hash de senha de usuário (Argon2id, issue #30). Senha escolhida por pessoa
  # exige função lenta e com sal — o `hmac.compare_digest` de `api/auth.py`
  # resolve o problema oposto (segredo de máquina, de alta entropia).
  "argon2-cffi>=23.1,<25",
  # TOTP do segundo fator (RFC 6238, issue #35). Biblioteca pequena e sem
  # dependência transitiva, usada só para derivar o código de um passo
  # (`TOTP.at`) e montar a URI `otpauth://` do QR code — a janela de aceitação
  # e o anti-replay são nossos, em `auth/mfa.py`, porque `TOTP.verify` devolve
  # booleano e o passo aceito é justamente o que precisamos guardar.
  "pyotp>=2.9,<3",
  # Cifra do segredo TOTP em repouso (ADR 0008). `Fernet`/`MultiFernet`:
  # AES-128-CBC com HMAC-SHA256 e timestamp, autenticado — não é primitiva
  # solta, é uma construção fechada que não deixa escolher modo nem IV. O
  # `MultiFernet` é o que permite rotacionar a chave sem downtime, como
  # `API_KEYS` já faz para a chave de máquina. É dependência NOVA de verdade —
  # não estava no `uv.lock` nem como transitiva —, então a imagem do Compose
  # precisa de `docker compose --profile tools build` antes do próximo `up`.
  "cryptography>=44,<47",
]

[dependency-groups]
dev = [
  "pytest>=8.4,<9",
  "pytest-cov>=6.2,<7",
  "ruff>=0.12,<1",
  "mypy>=1.17,<2",
  "types-boto3>=1.40,<2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/homecareos"]

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers --disable-warnings"
markers = [
  # Testes que exigem Postgres e MinIO de verdade (`docker compose up -d`).
  # Eles se pulam sozinhos quando os serviços não respondem, para a suíte
  # continuar utilizável sem container; a marca existe para poder selecioná-los
  # (`-m integration`) ou excluí-los (`-m 'not integration'`) explicitamente.
  "integration: requer Postgres e MinIO reais",
]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true
show_error_codes = true
mypy_path = "src"
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = ["fitz", "boto3.*", "botocore.*"]
ignore_missing_imports = true
